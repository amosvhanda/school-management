/**
 * OpenAPI-backed types from Laravel Scribe.
 *
 * Regenerate after backend API changes:
 *   npm run types:api
 *
 * Import path types for strongly typed endpoints:
 *   import type { paths } from '@/types/api.generated'
 */
export type { paths, components } from './api.generated'
